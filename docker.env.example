# Optional Docker-only live LLM key.
# Leave empty for safe local Docker validation with no live Groq calls.
# Do not copy real keys into docker-compose.yml or commit docker.env.
CAMPUSAI_DOCKER_GROQ_API_KEY=
