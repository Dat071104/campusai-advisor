# Non-secret Streamlit UI defaults (CampusAI Advisor MVP).
# Do not add secrets here; use local .env or Streamlit Community Cloud Secrets.

[theme]
base = "dark"
primaryColor = "#38bdf8"
backgroundColor = "#080b12"
secondaryBackgroundColor = "#111827"
textColor = "#eef2ff"
font = "sans serif"

[server]
headless = true
